name: qiniu_flutter_sdk_example
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependency_overrides:
  qiniu_sdk_base:
    path: ../../base/

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.5
  qiniu_flutter_sdk:
    path: ../

dev_dependencies:
  flutter_lints:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true