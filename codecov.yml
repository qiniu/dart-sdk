coverage:
  status:
    project:
      default:
        # pr 里面很多测试用例都被跳过了，所以这个 target 没有意义。如果能让需要 token 的测试在 pr 跑起来可以修改这个 target
        target: 0%
        branches: 
          - master
        informational: true
